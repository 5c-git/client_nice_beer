import './catalog-amount.scss';

// let checker = true;

// const catalogAmountInit = () => {
//   const catalogAmount = document.querySelector('.catalog-amount');
//   if (catalogAmount) {
//     if (checker) {
//       catalogAmount.classList.add('catalog-amount--hide');
//     } else {
//       catalogAmount.classList.remove('catalog-amount--hide');
//     }
//     const toggle = catalogAmount.querySelector('.catalog-amount__toggle');
//     toggle.addEventListener('click', () => {
//       catalogAmount.classList.toggle('catalog-amount--hide');
//       checker = !checker;
//     });
//   }
// };

// export default catalogAmountInit;
